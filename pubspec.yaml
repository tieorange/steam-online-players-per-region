name: arc_raiders_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  get_it: ^7.6.7
  injectable: ^2.3.2
  dio: ^5.4.1
  retrofit: ">=4.0.0 <4.9.0" # Keep below 4.9.0 to exclude DartMappable
  dartz: ^0.10.1
  freezed_annotation: any
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.2
  intl: ^0.19.0
  logger: ^2.0.2+1
  url_launcher: ^6.2.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: any
  freezed: any
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1
  retrofit_generator: ^9.0.0 # Use 9.x series which works with Retrofit 4.8.x
  bloc_test: ^9.1.7
  mocktail: ^1.0.3
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
